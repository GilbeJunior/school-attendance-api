const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src', 'modules', 'database', 'connections', 'database.js'),
  'migrations-path': path.resolve(__dirname, 'src', 'modules', 'database', 'migrations'),
};